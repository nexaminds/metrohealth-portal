{
  "name": "metrohealth-patient-portal",
  "version": "4.2.1",
  "description": "MetroHealth patient portal — registration validator (excerpt)",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "express": "^4.19.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.0.0",
    "@types/express": "^4.17.0",
    "vitest": "^2.1.0"
  }
}
